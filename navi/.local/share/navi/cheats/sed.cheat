% sed

# Print a specific line from a file
sed -n <number>p <file>

# Delete a specific line from a file
sed -n <number>d <file>

$ file: find . -maxdepth 1 -type f | cut -c3- --- --header "select a file"
; `:` does nothing, it is basically to create a dependency with `file`, and `echo` prevents a bug 
$ number: : <file> && echo ""
