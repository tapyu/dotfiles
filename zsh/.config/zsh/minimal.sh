# This is a minimal .zshrc setup, which is intended to
# test a plugin or a functionality in a minimal .zshrc
# configuration. To use it, symlink this zsh script:
# ln -sf minimal.sh .zshrc
# this will change `.zshrc` from `zshrc.sh` to `minimal.sh` 
